버니 샌더스 숏츠 아카이브 자동 발행을 지금 1회 수행해줘.

반드시 이 저장소 CLAUDE.md의 "버니 샌더스 숏츠 아카이브 (12시간마다 자동 발행)" 절차를 그대로 따른다:
1) `node scripts/bernie-next.mjs --json` 로 다음 후보를 받는다. `{"done":true}` 면 아무 것도 하지 말고 종료.
2) 영문 스크립트가 '버니 샌더스 본인의 발언/연설'이 아니면 `node scripts/bernie-next.mjs --skip <id> "사유"` 후 1)로 돌아가 다음 후보를 받는다(최대 5회 시도).
3) 영문 자동자막을 자연스러운 한국어로 번역(없는 내용 지어내지 않기, 중립적으로).
4) `src/content/blog/bernie-<id>.md` 를 CLAUDE.md 규칙대로 작성(frontmatter에 category: bernie, draft: false, videoId/videoUrl/videoDate 정확히, 본문은 썸네일→영문→한국어→출처).
5) 빌드가 깨지지 않는지 `npm run build` 로 확인한 뒤 `git add . && git commit -m "bernie: <제목>" && git push`.
6) 발행 후 텔레그램으로 한국어 알림 1줄(글 제목 + URL 예상경로 /blog/bernie-<id>/)을 보낸다.

주의: 한 번 실행에 글 1개만 발행한다. 기존 글은 건드리지 않는다.
