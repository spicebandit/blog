---
import { categoryName, type CategorySlug } from '../lib/categories';
import { formatDateKRCompact } from '../lib/format';

interface PostData {
  id: string;
  title: string;
  category: CategorySlug;
  pubDate: Date;
  heroImage?: string;
  author?: string;
}

interface Props {
  posts: PostData[];
}

const { posts } = Astro.props;
---
<section class="brief-section">
  <div class="brief-header">
    <h2 class="brief-title">Latest Brief</h2>
    <a href="/blog/" class="brief-more">전체 보기 →</a>
  </div>
  <ul class="brief-list" role="list">
    {posts.map((post) => {
      const dateLabel = formatDateKRCompact(post.pubDate);
      return (
        <li class="brief-item">
          <a href={`/blog/${post.id}/`} class="brief-link">
            <div class:list={['brief-thumb', !post.heroImage && `thumb-${post.category}`]}>
              {post.heroImage
                ? <img src={post.heroImage} alt="" loading="lazy" />
                : <div class="thumb-placeholder" aria-hidden="true"></div>
              }
            </div>
            <div class="brief-body">
              <span class:list={['cat-badge', post.category]}>{categoryName(post.category)}</span>
              <p class="brief-headline">{post.title}</p>
              <span class="brief-meta">
                {post.author && <>{post.author} · </>}<time datetime={post.pubDate.toISOString()}>{dateLabel}</time>
              </span>
            </div>
          </a>
        </li>
      );
    })}
  </ul>
</section>

<style>
.brief-section {
  margin-bottom: 40px;
}

.brief-header {
  display: flex;
  align-items: baseline;
  justify-content: space-between; /* 전체보기는 바 오른쪽 끝 */
  gap: 14px;
  margin-bottom: 16px;
  padding-bottom: 12px;
  border-bottom: 2px solid var(--line);
  position: relative;
}
.brief-header::after {
  content: '';
  position: absolute;
  left: 0;
  bottom: -2px;
  width: 48px;
  height: 2px;
  background: var(--accent);
  border-radius: 1px;
}
.brief-title {
  font-family: var(--font-serif);
  font-size: 1.25rem;
  font-weight: 700;
  letter-spacing: -0.015em;
  color: var(--ink);
  margin: 0;
}
.brief-more {
  font-size: 0.8125rem;
  color: var(--accent);
  font-weight: 600;
  text-decoration: none;
}
.brief-more:hover { text-decoration: underline; }

.brief-list {
  list-style: none;
  padding: 0;
  margin: 0;
}

.brief-item {
  border-bottom: 1px solid var(--line-2, #f3f4f6);
}
.brief-item:last-child { border-bottom: none; }

.brief-link {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 14px 0;
  text-decoration: none;
  color: inherit;
}
.brief-link:hover { text-decoration: none; }
.brief-link:hover .brief-headline { color: var(--accent); }

.brief-thumb {
  flex-shrink: 0;
  width: 72px;
  height: 72px;
  border-radius: 8px;
  overflow: hidden;
}
.brief-thumb img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}
.thumb-placeholder { width: 100%; height: 100%; }
.brief-thumb .thumb-placeholder { background: linear-gradient(135deg, var(--accent-soft), var(--bg-2)); border: 1px solid var(--line); }

.brief-body {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 5px;
}

/* 신문형: 배경 없는 레드 캡스 라벨 */
.cat-badge {
  display: inline-block;
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--accent);
  align-self: flex-start;
}

.brief-headline {
  margin: 0;
  font-family: var(--font-serif);
  font-size: 1rem;
  font-weight: 600;
  line-height: 1.32;
  letter-spacing: -0.015em;
  color: var(--ink);
  word-break: keep-all;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  overflow: hidden;
  transition: color .15s;
}

.brief-meta {
  font-size: 12px;
  color: var(--muted);
}

@media (max-width: 480px) {
  .brief-thumb {
    width: 56px;
    height: 56px;
  }
}
</style>
